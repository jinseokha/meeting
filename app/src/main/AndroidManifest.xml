<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Meeting"
        tools:targetApi="31">
        <activity
            android:name=".view.main.MainActivity"
            android:exported="true"
            android:label="@string/screen_meet"
            android:theme="@style/Theme.Meeting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.edit.MeetEditActivity"
            android:exported="true"
            android:label="@string/screen_meet_edit"
            android:theme="@style/Theme.Meeting">
        </activity>

        <activity
            android:name=".view.meet.MeetAddActivity"
            android:exported="true"
            android:label="@string/screen_meet_add"
            android:theme="@style/Theme.Meeting"/>
    </application>

</manifest>